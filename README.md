MyTasks — это кроссплатформенное() desktop-приложение на Python для управления задачами, привычками и личной продуктивностью.  
Приложение объединяет **To‑Do список**, **трекер привычек**, **матрицу Эйзенхауэра**, **таймер Помодоро** и **настройки приложения** в одном интерфейсе.

Проект построен с использованием фреймворка Flet, что позволяет создавать современные UI‑приложения на Python.

## Возможности

### 📋 To‑Do List
- Создание, удаление и завершение задач
- Подзадачи для каждой задачи
- Описание задачи
- Приоритет задачи (коэффициент 1–4)
- Интеграция с матрицей Эйзенхауэра

### 📊 Матрица Эйзенхауэра
- Автоматическое распределение задач по 4 квадрантам:
  1. Срочно и важно
  2. Не срочно, но важно
  3. Срочно, но не важно
  4. Не срочно и не важно
- Отображаются только незавершённые задачи

### 🔁 Трекер привычек
- Создание и удаление привычек
- Учёт количества выполнений
- Редактирование названия привычки
- Хранение статистики

### ⏱ Помодоро
- Настраиваемое время в минутах
- Старт / пауза / сброс таймера
- Фоновая работа таймера
- Звуковое уведомление по окончании (Windows)

### ⚙️ Настройки
- Переключение светлой и тёмной темы
- Сохранение темы между запусками
- Информация о приложении

---

## Архитектура проекта

Проект имеет модульную структуру:

```
.
├── main.py                 # Точка входа приложения
├── requirements.txt        # Зависимости проекта
├── module/
│   ├── __init__.py
│   ├── data_manager.py     # Загрузка и сохранение данных
│   ├── todo_list.py        # To‑Do список
│   ├── habit_tracker.py    # Трекер привычек
│   ├── eisenhower_matrix.py# Матрица Эйзенхауэра
│   ├── pomodoro.py         # Таймер Помодоро
│   └── settings.py         # Настройки
└── data/
    └── app_data.json       # Хранилище данных
```

---

## Хранение данных

Все данные приложения сохраняются локально в файле:

```
data/app_data.json
```

### Структура данных

```json
{
  "tasks": [],
  "habits": [],
  "theme": "light",
  "pomodoro": {
    "running": false,
    "seconds": 1500,
    "time_input_value": "25",
    "end_ts": null
  }
}
```

---

## Основные компоненты

### DataManager
Централизованный менеджер данных:
- `load_data()` — загрузка данных
- `save_data()` — сохранение данных
- Используется всеми модулями

### Навигация
Навигация реализована через `NavigationRail`:
- To‑Do list
- Habit Tracker
- Eisenhower Matrix
- Pomodoro
- Settings

Контент подгружается динамически без перезапуска приложения.

---

## Установка и запуск

### 1. Клонирование проекта
```bash
git clone <https://github.com/S2FFY/MyTask.git>
cd hpp
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Запуск приложения
```bash
python main.py
```

---

## Требования

- Python **3.11.4+**
- Поддерживаемые ОС:
  - Windows
  - macOS
  - Linux

---

## Используемые технологии

- **Python**
- **Flet**
- **JSON** (локальное хранилище)
- **AsyncIO** (таймер Помодоро)

---

## Версия

**v1.0.0**

---

## Возможные улучшения

- Авторизация пользователя
- Синхронизация данных (cloud)
- Статистика и аналитика привычек
- Уведомления
- Экспорт данных

---

## Лицензия

Проект предназначен для учебных и личных целей.
